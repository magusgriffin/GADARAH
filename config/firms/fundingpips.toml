# Legacy comparison profile only.
# Official FundingPips products and terms changed; do not treat this file as a
# production-ready U.S. cTrader bot profile without a fresh terms review.
[firm]
name = "FundingPips"
challenge_type = "2step"
profit_target_pct = 6.0
daily_dd_limit_pct = 4.0
max_dd_limit_pct = 6.0
dd_mode = "trailing"
min_trading_days = 3
news_trading_allowed = true
max_positions = 5
profit_split_pct = 80.0

[broker]
host = "live.ctraderapi.com"
port = 5035
client_id_env = "GADARAH_FP_CLIENT_ID"
client_secret_env = "GADARAH_FP_CLIENT_SECRET"
